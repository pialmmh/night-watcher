# Fail2Ban filter for ModSecurity - BDCOM
# Matches ModSecurity log entries to extract offending client IPs

[Definition]
failregex = ^.*\[client <HOST>\].*ModSecurity:.*$
            ^.*client: <HOST>.*ModSecurity:.*$
            ^.*"client_ip":"<HOST>".*$

ignoreregex =
