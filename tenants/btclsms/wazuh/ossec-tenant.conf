<ossec_config>

  <!-- BTCL SMS tenant-specific Wazuh configuration -->
  <!-- Merged with ossec-base.conf at container startup -->

  <!-- Alert email (placeholder - enable when SMTP is configured) -->
  <!--
  <global>
    <email_notification>yes</email_notification>
    <smtp_server>smtp.btcliptelephony.gov.bd</smtp_server>
    <email_from>wazuh@btcliptelephony.gov.bd</email_from>
    <email_to>security@telcobright.com</email_to>
    <email_maxperhour>12</email_maxperhour>
  </global>
  -->

  <!-- Active response: block high-severity attackers -->
  <!-- Requires firewall-drop command to be defined; enable after validating -->
  <!--
  <active-response>
    <command>firewall-drop</command>
    <location>local</location>
    <rules_id>100104,100204,100205</rules_id>
    <timeout>3600</timeout>
  </active-response>
  -->

  <!-- BTCL-specific log collection -->
  <localfile>
    <log_format>syslog</log_format>
    <location>/var/log/security-bundle/watchdog.log</location>
  </localfile>

  <!-- Registration password -->
  <auth>
    <use_password>yes</use_password>
  </auth>

</ossec_config>
